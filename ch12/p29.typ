#import "@local/elektroedde:0.1.0": *
#show: problem

=
(a) Repeat Prob. 12.2(a) using the (incorrect) definition $vb(p)= m vb(u)$, but with the (correct) Einstein velocity addition rule. Notice that if momentum (so defined) is conserved in $cal(S)$, it is not conserved in $ol(cal(S))$. Assume all motion is along the $x$ axis.

(b) Now do the same using the correct definition, $vb(p)= m vb(eta)$. Notice that if momentum (so defined) is conserved in $cal(S)$, it is automatically also conserved in $ol(cal(S))$. [Hint: Use Eq. 12.43 to transform the proper velocity.] What must you assume about relativistic energy?